# Date Picker

A dependency-free, framework-agnostic date and time picker built in TypeScript, with a thin Vue 3 adapter.

Repository initialization. The complete library and demo are added in the next direct commit.
