name: Use deterministic picker opening in browser audit

on:
  push:
    branches: [browser-recheck]
    paths:
      - .github/workflows/patch-browser-open-api.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Open custom audit pickers through their public API
        run: |
          python3 <<'PY'
          from pathlib import Path
          path = Path('tests/browser-audit.mjs')
          text = path.read_text()
          old = '''async function openPicker(id) {
            const popover = page.locator(`#${id} .sdp-datepicker__popover`)
            if (await popover.evaluate(node => node.hidden)) {
              await page.locator(`#${id} .sdp-datepicker__trigger`).click()
            }
            await page.waitForFunction(id => {
              const current = document.querySelector(`#${id} .sdp-datepicker__popover`)
              return current && !current.hidden
            }, id)
          }
          '''.replace('          ', '')
          new = '''async function openPicker(id) {
            await page.evaluate(id => window.__datePickerAudit.pickers[id].open(), id)
            await page.waitForFunction(id => {
              const current = document.querySelector(`#${id} .sdp-datepicker__popover`)
              return current && !current.hidden
            }, id)
          }
          '''.replace('          ', '')
          if old not in text:
              raise SystemExit('openPicker API patch target not found')
          path.write_text(text.replace(old, new, 1))
          PY

      - name: Commit patch
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add tests/browser-audit.mjs
          git diff --cached --check
          git commit -m "Use public API to open browser audit pickers"
          git push
